swarm:
  logging: INFO
  datasources:
    data-sources:
      ProductDS:
        driver-name: postgresql
        connection-url: jdbc:postgresql://catalog-database:5432/catalog
        user-name: swarm
        password: password
