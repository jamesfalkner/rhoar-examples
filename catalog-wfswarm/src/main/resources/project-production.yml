swarm:
  logging: INFO
  datasources:
    data-sources:
      ProductDS:
        driver-name: postgresql
        connection-url: jdbc:postgresql://inventory-database:5432/inventory
        user-name: swarm
        password: password
